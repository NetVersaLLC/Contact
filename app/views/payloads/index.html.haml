.row
  .col-xs-6
    = select_tag(:payloads_site, options_from_collection_for_select(Site.all, :id, :name))
    = select_tag(:payloads_mode, options_from_collection_for_select(Mode.all, :id, :name))
    = button_tag("Load", :id => :payloads_load)
    = button_tag("Save", :id => :payloads_save)
  .col-xs-6
.row
  .col-xs-12 
    %form#payloads
      #payloads_tree.dd
    = javascript_include_tag "jquery.nestable.min"
