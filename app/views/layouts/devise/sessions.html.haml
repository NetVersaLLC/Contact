!!!
%html{:lang => "en"}
  =render 'layouts/head'
  %body.login-layout
    .main-container 
      .main-content
        .row
          .col-sm-10.col-sm-offset-1
            .login-container{ class: "#{controller_name}"}
              .center 
                =image_tag current_label.logo, class: "login-logo" if current_label
              .space-6
              .position-relative
                = yield
