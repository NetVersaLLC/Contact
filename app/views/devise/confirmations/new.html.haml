= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, :class => 'nice' }) do |f|
  = devise_error_messages!
  %fieldset
    %h5 Resend confirmation instructions
    = f.label :email
    = f.email_field :email, :class => 'input-text'
    = f.submit "Resend confirmation instructions", :class => "nice small radius blue button"
    %br

= render "links"
